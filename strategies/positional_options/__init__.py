"""Positional options strategies — the runtime group whose positions may
remain open across trading sessions under a durable ``cycle_id`` (spec:
``common.engine.positional``). ``weekly_delta_neutral`` is the first and,
per CLAUDE.md, only real strategy in this group.
"""
