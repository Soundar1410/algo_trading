"""Runtime groups.

``intraday_options`` is the only one with a supervisor and worker.
``positional_options`` (Phase 5) is inert scaffolding — its own ``__init__.py``
explains why the rest is deferred to Phase 6/9, not merely unwired.
"""
